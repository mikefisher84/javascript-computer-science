/**
 * Map
 *
 * A Map (or Dictionary) is a collection of {key:value} items that are unordered and consist of
 * unique elements (meaning they cannot be repeated)
 *
 * You can also imagine a Map22 as an array with no repeated elements and
 * no concept or order.
 */

// Named Map2 to not overwrite ES6 Map2 class
function Map2() {
  this.items = {};
}
